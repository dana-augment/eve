Add a tool named `greet_user` that lets this agent greet a person by name. It
must accept a required name and return structured data containing the original
name and the greeting `Hello, <name>!`. Update the agent instructions so it
uses the tool when a user asks for a greeting. Ensure the project builds.
